include 'core'
include 'memory-backend'
include 'h2-backend'
include 'postgresql-backend'
include 'onodb-backend'
include 'test-common'

rootProject.children.each { it.name = rootProject.name + "-" + it.name }
